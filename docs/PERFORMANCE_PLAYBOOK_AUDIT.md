# تشخيص الأداء — Next.js Playbook vs المشروع

مراجعة سريعة لمشروع BeyondRounds مقابل الـ 10 نقاط.

---

## 1️⃣ App Router + Server Components

| الحالة | التفاصيل |
|--------|----------|
| ✅ | استخدام App Router (`app/`) |
| ✅ | صفحات الماركتينغ: Server Components + `getDictionary(locale)` في السيرفر |
| ✅ | `revalidate = 3600` على صفحات (marketing, about, pricing, …) |
| ⚠️ | الصفحات التفاعلية (auth, onboarding, dashboard) كلها `"use client"` — منطقي لأنها تحتاج state/hydration. لا تحوّل كل شيء لـ client إلا حيث لازم |

---

## 2️⃣ حجم JavaScript

| الحالة | التفاصيل |
|--------|----------|
| ✅ | `@next/bundle-analyzer` مضبوط — شغّل `ANALYZE=true npm run build` ثم افتح التقرير |
| ✅ | `optimizePackageImports: ['lucide-react', 'date-fns', 'recharts']` في next.config |
| ✅ | Route-based provider splitting: QueryClient, Locale, Tooltip، و Toasters تُحمّل ديناميكياً فقط على routes الـ app |
| ✅ | `removeConsole` في production |
| 🎯 | **افعل:** بعد كل تغيير كبير شغّل `ANALYZE=true npm run build` وتأكد أنه لا يوجد chunk > 150KB غير مبرر |

---

## 3️⃣ UI من المشروع (ليس مكتبة ضخمة)

| الحالة | التفاصيل |
|--------|----------|
| ✅ | الاستيراد من `@/components/ui/` (نسخ محليّة من shadcn) — لا استيراد كامل لمكتبة خارجية |

---

## 4️⃣ Lazy load للمكوّنات الثقيلة

| الحالة | التفاصيل |
|--------|----------|
| ✅ | QueryClientWrapper, LocaleProviderWrapper, TooltipProviderWrapper → `dynamic(..., { ssr: false })` |
| ✅ | Toaster و Sonner → dynamic + ظهور بعد 600ms |
| ✅ | Charts/Editors إن وُجدت يُفضّل أن تبقى داخل dynamic |

---

## 5️⃣ الصور

| الحالة | التفاصيل |
|--------|----------|
| ✅ | الهيرو: `next/image` في HeroImageServer مع `sizes` و `fetchPriority="high"` |
| ✅ | صور الهيرو: `.webp` + نسخة mobile |
| ⚠️ | Hero: `unoptimized` — إن الملفات مُحسّنة مسبقاً (مثلاً < 200KB) فمقبول؛ إن لا، أزل unoptimized واترك Next يضغط |
| ✅ | أقسام Onboarding (avatar/license preview): `<img>` لـ blob URLs فقط — صحيح ولا يحتاج next/image |

**قاعدة:** أي صورة ثابتة جديدة (مثل هيرو أو بانرات) استخدم لها `next/image` مع width/height أو fill وبدون unoptimized إن أمكن.

---

## 6️⃣ الخطوط بدون blocking

| الحالة | التفاصيل |
|--------|----------|
| ✅ | `next/font/google`: DM_Sans و Space_Grotesk |
| ✅ | `display: 'swap'` و `preload: false` — لا blocking للرندر |

---

## 7️⃣ CSS (Tailwind)

| الحالة | التفاصيل |
|--------|----------|
| ✅ | `content: ["./app/**/*.{ts,tsx}", "./src/**/*.{ts,tsx}"]` — لا استمبار لمجلدات زائدة |

---

## 8️⃣ Hydration

| الحالة | التفاصيل |
|--------|----------|
| ✅ | الصفحات = Server؛ الـ client فقط في layout (Providers) والمكوّنات التفاعلية (Auth, Onboarding, …) |
| ✅ | لا دمج `"use client"` مع `async function` في نفس الملف |

---

## 9️⃣ Cache

| الحالة | التفاصيل |
|--------|----------|
| ✅ | صفحات الماركتينغ: `export const revalidate = 3600` (أو 86400 للـ terms/privacy) |
| ✅ | API الـ location (countries, states, cities): `next: { revalidate: 86400 }` |
| 🎯 | أي fetch جديد لبيانات ثابتة أو شبه ثابتة: استخدم `fetch(url, { next: { revalidate: 60 } })` أو `revalidate` في الصفحة |

---

## 10️⃣ القياس الفعلي

| الإجراء | الأمر / الخطوة |
|---------|-----------------|
| تحليل الحزم | `ANALYZE=true npm run build` ثم فتح الرابط الذي يظهر |
| Lighthouse موبايل | Chrome DevTools → Lighthouse → Mobile → Run |
| أهداف تقريبية | LCP < 2.5s، JS < 500KB للماركتينغ، صور ثابتة < 200KB للصفحة الرئيسية |

---

## خلاصة

- البنية الحالية متوافقة مع أغلب نقاط الـ Playbook (App Router، fonts، Tailwind، cache، route-based splitting، dynamic للـ providers والـ toasters).
- الخطوة التالية: تشغيل **Lighthouse Mobile** على الصفحة الرئيسية و/أو صفحة Auth أو Onboarding، وإرسال لقطة (أو الأرقام: LCP، TBT، حجم JS، حجم الصور). عندها يمكن «تشخيص جراحي» لأين تضيع الميلي ثواني بالضبط (مثلاً: chunk معيّن، صورة، خط، أو مكوّن client ثقيل).
