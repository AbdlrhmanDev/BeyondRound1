# إعداد متغيرات البيئة (Environment Variables)

## المشكلة
إذا رأيت رسالة الخطأ:
```
❌ Configuration Error: Missing Supabase URL. Please set VITE_SUPABASE_URL in your environment variables.
```

هذا يعني أن متغيرات البيئة غير محددة بشكل صحيح.

## الحل السريع

### 1. للتطوير المحلي (Local Development)

أنشئ ملف `.env.local` في المجلد الرئيسي للمشروع:

```env
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here
```

**كيفية الحصول على القيم:**

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **Settings** → **API**
4. انسخ:
   - **Project URL** → استخدمه كـ `VITE_SUPABASE_URL`
   - **anon/public key** → استخدمه كـ `VITE_SUPABASE_PUBLISHABLE_KEY`

**بعد إنشاء الملف:**
```bash
# أوقف السيرفر إذا كان يعمل (Ctrl+C)
# ثم أعد تشغيله
npm run dev
```

### 2. للنشر على Vercel

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر مشروعك
3. اذهب إلى **Settings** → **Environment Variables**
4. أضف المتغيرات التالية:

```
VITE_SUPABASE_URL = https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY = your_anon_key_here
```

5. بعد إضافة المتغيرات:
   - اذهب إلى **Deployments**
   - اضغط على **⋯** بجانب آخر deployment
   - اختر **Redeploy**

### 3. التحقق من الإعداد

بعد إعداد المتغيرات، افتح المتصفح وتحقق من:
- لا توجد رسائل خطأ في Console (F12)
- التطبيق يعمل بشكل طبيعي

## ملاحظات مهمة

- ⚠️ **لا ترفع ملف `.env.local` إلى Git** - إنه موجود في `.gitignore`
- ✅ استخدم `.env.local` للتطوير المحلي فقط
- ✅ استخدم Vercel Environment Variables للنشر
- 🔄 يجب إعادة تشغيل dev server بعد تعديل `.env.local`

## استكشاف الأخطاء

### المشكلة: التطبيق لا يزال يظهر نفس الخطأ

**الحل:**
1. تأكد من أن الملف اسمه `.env.local` (وليس `.env` أو `.env.example`)
2. تأكد من أن المتغيرات تبدأ بـ `VITE_`
3. أعد تشغيل dev server: `npm run dev`
4. امسح cache المتصفح (Ctrl+Shift+R)

### المشكلة: على Vercel لا يعمل

**الحل:**
1. تأكد من إضافة المتغيرات في Vercel Dashboard
2. تأكد من أن الأسماء مطابقة تماماً (حساسة لحالة الأحرف)
3. قم بـ Redeploy بعد إضافة المتغيرات
4. تحقق من Build Logs في Vercel
